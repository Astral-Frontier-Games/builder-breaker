import type { ThemeType } from "grommet";

const theme: ThemeType = {
    global: {
      font: {
        family: 'Roboto',
        size: '18px',
        height: '20px',
      },
    },
  };

  export default theme